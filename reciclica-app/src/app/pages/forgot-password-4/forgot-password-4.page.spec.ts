import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ForgotPassword4Page } from './forgot-password-4.page';

describe('ForgotPassword4Page', () => {
  let component: ForgotPassword4Page;
  let fixture: ComponentFixture<ForgotPassword4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ForgotPassword4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
