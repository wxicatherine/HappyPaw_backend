import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ForgotPassword3Page } from './forgot-password-3.page';

describe('ForgotPassword3Page', () => {
  let component: ForgotPassword3Page;
  let fixture: ComponentFixture<ForgotPassword3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ForgotPassword3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
