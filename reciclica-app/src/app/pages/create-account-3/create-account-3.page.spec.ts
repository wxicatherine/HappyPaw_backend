import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CreateAccount3Page } from './create-account-3.page';

describe('CreateAccount3Page', () => {
  let component: CreateAccount3Page;
  let fixture: ComponentFixture<CreateAccount3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CreateAccount3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
